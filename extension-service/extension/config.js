// Extension config. Set TOKEN to the value the server prints on startup
// (also saved in extension-service/.token). ENDPOINT must match the server.
window.CLAUDE_CFG = {
  ENDPOINT: "http://127.0.0.1:8787/run",
  TOKEN: "PASTE_TOKEN_HERE",
};
