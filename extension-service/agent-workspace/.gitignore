# The agent runs here (cwd for gemini); ignore everything it generates
# (GEMINI.md is regenerated on startup; screenshots/scratch come from tasks).
# Keep only this file and the pinned auth settings.
*
!.gitignore
!.gemini/
!.gemini/settings.json
